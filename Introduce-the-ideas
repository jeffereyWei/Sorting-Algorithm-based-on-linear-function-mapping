线性映射的排序算法：
原理： 当输入乱序的数组时，假如已经有一个Y=X这样的函数，那么依次把乱序数组中的每一个元素都当做X，输入到Y=X这样的函数列表中，再从小到大来读取Y序列的每一个元素。原来乱序的数组就完成了排序。在当前的各种编程语言中，数组的定义是按照自然数顺序排列的，那么就可以把数组作为Y函数来使用，把数组的索引作为输入序列X来使用。这样，把输入序列X中的每一个元素作为数组的索引，并在Y[X]数组中标识该值。然后根据Y[X]数组的标识从小到大输出数组的索引，就完成了排序。

